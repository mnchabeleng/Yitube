<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
      <a class="navbar-brand" href="/"><img src="/img/icon.png" alt=""> Yitube</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    
      <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item<%= title === 'Yitube' ? ' active' : null %>">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item<%= title === 'Browse' ? ' active' : null %>">
            <a class="nav-link" href="/browse">Browse</a>
          </li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <% if((typeof user !== 'undefined') && user) { %>
            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li>
          <% } else { %>
            <li class="nav-item<%= title === 'Login' ? ' active' : null %>">
              <a class="nav-link" href="/login">Login</a>
            </li>
            <li class="nav-item<%= title === 'Signup' ? ' active' : null %>">
              <a class="nav-link" href="/signup">Signup</a>
            </li>
          <% } %>
        </ul>
      </div>
  </div>
</nav>